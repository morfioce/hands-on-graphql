type Query {
  ping: String
  me: User
  user(id: ID!): User
}

type User {
  id: ID!
  name: String!
  email: String!
  tweets: [Tweet!]!
  followers: [User!]!
}

type Tweet {
  id: ID!
  text: String!
  author: User!
}